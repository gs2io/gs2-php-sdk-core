## Customize the test machine
machine:
  php:
    version: 5.6.22

test:
  override:
    - phpunit --coverage-html=reports/coverage_html/ --coverage-clover=reports/coverage.xml tests/
  post:
    - cp -rf reports $CIRCLE_ARTIFACTS
    - cp coverage.xml $CIRCLE_ARTIFACTS